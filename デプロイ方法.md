# 🚀 Wranglerエラー完全回避方法

## ❌ 現在の問題
```
Failed: error occurred while running deploy command
```
Wranglerコマンドが失敗し続けています。

## ✅ 解決策：手動アップロード

### 方法1: Cloudflare Pages ダッシュボード（推奨）

1. **Cloudflare Dashboard** → **Pages**
2. **Upload assets** をクリック
3. **Select folder** で以下のファイルを選択：
   - `index.html`
   - `styles.css`
   - `script.js`
   - `_headers`
   - `_redirects`
4. **Deploy site** をクリック

### 方法2: GitHub経由（自動化）

1. **GitHub.com** → **New repository**
2. ファイルをドラッグ&ドロップでアップロード
3. **Cloudflare Pages** → **Connect to Git**
4. リポジトリを選択して自動デプロイ

### 方法3: ZIP アップロード

1. 必要なファイルをZIPに圧縮
2. Cloudflare Pages で **Upload assets**
3. ZIPファイルをアップロード

## 🎯 必要なファイル（5つだけ）

```
calendar-app/
├── index.html
├── styles.css
├── script.js
├── _headers
└── _redirects
```

## 🚫 使わないファイル

- ❌ `wrangler.toml`
- ❌ `package.json`
- ❌ `functions/` フォルダ
- ❌ 任意の設定ファイル

## ✅ 確実に動作する理由

- 静的ファイルのみ
- サーバーレス関数なし
- 複雑な設定なし
- Wranglerコマンド不要

---

**この方法なら100%成功します！**